{
  "name": "@podman-desktop/docker-extension-api",
  "version": "0.0.1",
  "description": "API for Podman Desktop Docker extension",
  "repository": "https://github.com/podman-desktop/podman-desktop",
  "publishConfig": {
    "provenance": true,
    "access": "public"
  },
  "license": "Apache-2.0",
  "types": "./src/docker-extension-api.d.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "prepare": "",
    "clean": "rimraf lib *.tsbuildinfo",
    "build": "",
    "watch": "",
    "publish:next": "pnpm publish --registry=https://registry.npmjs.org/ --no-git-tag-version --new-version 0.0.1-\"$(date +%s)\""
  },
  "dependencies": {
    "@podman-desktop/api": "workspace:*"
  }
}
